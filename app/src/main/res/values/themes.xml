<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base app theme (Material 3). Pick ONE theme name to use everywhere. -->
    <style name="Theme.Mentora" parent="Theme.Material3.Light.NoActionBar">
        <!-- Optional edge-to-edge polish -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

</resources>
